# Indirect-Syscall
My implementation of an already existing technique for antivirus evasion.

Dynamically gets the SSN (system service number) from the dll loaded in ram then takes it and uses it to bypass antivirus hooks.
