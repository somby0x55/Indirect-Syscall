# Indirect-Syscall
My implementation of an already existing technique for antivirus evasion.

Dynamically gets the SSN (system service number) from the dll in ram then patches the function to evade antivirus hooks.
